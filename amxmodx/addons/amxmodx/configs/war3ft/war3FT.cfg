// 魔兽争霸 3 冰封王座 配置文件
// 该版本为 War3FT v3.0 RC13 由Osker Lee最终汉化修正
// 欢迎光临点通插件论坛：http://www.dt-club.net

// 经验值保存选项

wc3_save_xp		1		// 是否允许保存经验 (默认 0)
wc3_save_xp_db		0		// 数据经验保存方式 (0 = SQLite, 1 = MySQL)
wc3_save_by		1		// 设置数据保存模式?  steam id = 0, IP = 1, name = 2 (默认 0)
wc3_save_pruning	0		// 设置自动删除长时间没登陆的老玩家数据 (默认 0)
wc3_days_before_delete	31		// 设置数据库的数据经验存储时限 (默认 31)
wc3_save_end_round	0		// 每局结束后自动保存玩家数据 (可能会引起 lag，默认 1)

// SQL 数据库配置

wc3_sql_dbhost		"localhost"	// 主机地址
wc3_sql_dbuser		"root"		// 用户名
wc3_sql_dbpass		""		// 密码
wc3_sql_dbname		""		// 数据库名称

// 管理员选项

wc3_admin_flag		"m"		// 必须授予的标记才能获取经验值 (玩家默认权限，默认 m)

// 经验选项

wc3_xp_multiplier	0.5		// 设置升级所需的经验值倍数 (默认 1.0)
wc3_xp_weap_multiplier	1		// 使用某些武器时给与额外的奖励 (默认 1)
wc3_min_players		2		// 当服务器里最少有多少玩家的时候开启经验系统 (默认 2)
wc3_ignore_bots		0		// 当必需计算得到最小值的经验的时候，将要忽视机器人? (默认 0)
wc3_xp_auto_average	0		// 选项设定至 1, 玩家将会接受所有的玩家的平均值 (独享经验值，默认 0)
wc3_show_objectives	1		// 显示获取经验值的提示信息 (default is 1)
wc3_show_kill_obj	0		// 每杀死一个敌人显示获取的经验值 (每杀一个，默认 0)
wc3_xp_radius		750		// 各种事件给与临近队员的分享奖励距离 (营救，安放炸弹, 击杀 Vip，VIP 逃脱，默认 750)

// 游戏参数

wc3_races		9		// 种族数 (如种族少于 5个，就意味着 shopmenu2 将会被失效 (4 为标准的 War3)，默认 8)
wc3_grenade_protection	0		// 每局只允许购买一颗手雷 (默认 0)
wc3_spec_position	0		// 死亡视角文字信息的位置 (0 左边，1 右边, 默认 0)
wc3_buy_zone		0		// 超过时间后禁止购买商店里的物品 (默认 0)
wc3_buy_time		0		// 超过时间后禁止购买商店里的物品 (默认 0)
wc3_buy_dead		1		// 是否允许玩家死后购买物品 (默认 1)
wc3_show_player		0		// 瞄准队友和敌人时是否在屏幕中间显示信息 (不适用 DOD，默认 1)
wc3_query_client	0		// 检测全部玩家 cl_minmodel 模型 (默认 1)
wc3_cheats		0		// 作弊，犯规故意违反规定，如在游戏中 (默认 0)
wc3_psychostats		0		// 是否将wc3日志传送到 Psychostats 排名统计 (默认 0)
wc3_start_level		0		// 当新玩家进入游戏时平等级别 (默认 0)

// 图标选项(显示在玩家的头上面)

wc3_race_icon		1		// 是否允许玩家设置 /icons 显示玩家的种族信息 (默认 1)
wc3_level_icon		1		// 是否允许玩家设置 /icons 显示玩家的等级信息 (默认 1)

// 变色龙配置选项 -  详见 _chameleon.txt文件查看更多信息

wc3_cham_random		0		// 是否每次开局都随机更换技能, 是的话则下面4个选项起作用 (默认 1)
wc3_cham_skill1		4		// 第一类技能随机选取 (默认 14)
wc3_cham_skill2		18		// 第二类技能随机选取 (默认 0)
wc3_cham_skill3		27		// 第三类技能随机选取 (默认 6)
wc3_cham_ultimate	11		// 超绝技能随机选取 (默认 7)
wc3_cham_passive	35		// 使用者的被动的技能 (默认 20)

// 技能与大绝配置

wc3_ult_delay		10.0		// 每局开始时间 (在几秒钟之内) 开始延迟终极大绝  (默认 15.0)
wc3_ult_cooldown	20.0		// 终极大绝技能的恢复时间 (默认 35.0)
wc3_blink_protection	1		// 瞬移到地图的禁止区 (人行天桥，地图BUG的地方，默认 1)
wc3_blink_dizziness	1		// 什么类型的瞬移效果? (0 闪光效果，1 绿色屏幕效果，默认 0)
wc3_entangle_drop	0		// 当玩家被纠缠时是否掉落主要武器? (默认 0)

// 物品配置

wc3_glove_timer		20		// 灼烧手套，戴灼烧手套得到新的一枚手雷的时间间隔 (默认为 10)
wc3_glove_orc_damage	0		// 灼烧手套，在ka类的地图上禁止使用 (默认 0)
wc3_boots		0.13    	// 速度之靴，百分数增加移动速度。 (.10 = 10%，默认 0.10)
wc3_claw		10		// 暴风之锤，敌人攻击伤害值。 (默认 6)
wc3_health		25		// 生命之石，授予到恢复到额外的生命值。 (默认 15)
wc3_frost		45.0		// 冰冻之戒，使你的敌人减慢对你的攻击，至使瞬速攻击敌人。 (默认 125.0)
wc3_mask		0.45		// 免伤胸甲，你将会在每个攻击敌人中，吸取被受到的伤害值转变为生命值恢复。 (默认 0.3)
wc3_cloak		100		// 环雾披风，身体隐形潜行，数值越大将增加隐形精度 (默认 150)
wc3_sock		0.4		// 踏云腿甲，重力浮动模式的高度 (默认 0.5)
wc3_tome		150		// 亡者之书，使用经验值购买 (默认 50)

// 机器人配置

wc3_bot_buy_item	1		// 机器人在开局时购买物品的几率 (默认 is 0.33)

// 胜利之日 DOD 配置

wc3_dod_start_money	16000		// DOD 默认的金钱数额